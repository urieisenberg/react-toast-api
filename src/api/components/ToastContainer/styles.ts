import styled from "styled-components";

export const Wrapper = styled.div`
  position: fixed;
  z-index: 1;
  right: 0;
  top: 0;
`;

//     top: ${(props) => props.position.includes("top") && "0"};
//     bottom: ${(props) => props.position.includes("bottom") && "0"};
//     left: ${(props) => props.position.includes("left") && "0"};
//     right: ${(props) => props.position.includes("right") && "0"};
